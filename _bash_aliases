#!/bin/bash

alias be="bundle exec"
alias bs="bundle show"
alias vundle='nvim +PlugInstall +UpdateRemotePlugins +qa'

# for python
alias de="dae venv"
alias dja="django-admin.py"
alias djs="dj runserver"
alias djm="dj schemamigration --auto"
alias py='python'

# for utils
alias pkill='killall'
alias ls='ls --color=auto -X --group-directories-first'
alias gvim="gvim -f"
alias vsc="open -a 'Visual Studio Code'"
alias ll='ls -alF'
alias la='ls -A'
# alias df='df -hT'
alias l='ls -CF'
alias open='gnome-open'
alias pgrep='pgrep -l'
alias ggrep='git grep -n'
alias cdr='while [ ! -d .git ] && [ ! `pwd` = "/" ] && [ ! `pwd` = ~ ]; do cd ..; done;'
alias vi='nvim'
alias vim='nvim'

alias untar-gz='tar xvfz'
alias untar-bz2='tar xvfj'
alias untar='tar xvf'

alias ..='cd ..'

if [ "$KERNEL" = 'Darwin' ]; then
    # brew install coreutils
    alias ls='gls --color -h --group-directories-first --ignore="*.pyc"'
    # alias ctags='/usr/local/Cellar/ctags/5.8/bin/ctags'
    alias atom='open -a Atom'
    alias mvim='open -a MacVim'
    alias typora='open -a typora'
    unalias open
fi
